# Enable the rewrite engine
RewriteEngine On

<Directory /var/www/html>
    AllowOverride All
</Directory>

# Ensure .php extension is removed from URLs
# Redirect .php URLs to clean URLs
RewriteCond %{THE_REQUEST} \s/([^\s]+)\.php [NC]
RewriteRule ^ %1 [R=301,L]